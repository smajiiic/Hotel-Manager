// Placeholder — owned by another teammate. Replace with the real login form.
// Submit should call: const { /* login */ } = useAuth(); (sprint 2 wiring)
function LoginPage() {
  return <div className="placeholder-page">Login — owned by another teammate.</div>
}

export default LoginPage
