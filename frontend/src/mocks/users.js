// Mock fixture — current user for sprint 1. Hardcoded as reception so the role
// sees the full nav. Change `role` to 'cleaning' or 'manager' to test role-gated
// UI before swapping to a real session.

export const mockCurrentUser = { username: 'tarik', role: 'reception' }
